[pytest]
testpaths = backend/tests
python_files = test_*.py
markers =
    slow: pruebas que requieren un modelo entrenado o son más lentas.
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
