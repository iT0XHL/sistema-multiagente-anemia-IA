# ── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ipynb_checkpoints/

# ── Modelos / artefactos entrenados ──────────────────────
ml/saved_models/*.joblib
ml/saved_models/*.pkl
explainability/outputs/*.png
explainability/outputs/*.json

# ── Datos pesados (se versiona solo la muestra) ──────────
data/raw/*.csv
data/processed/*.csv
!data/sample/

# ── Node / Frontend ──────────────────────────────────────
node_modules/
frontend/node_modules/
frontend/dist/
frontend/public/styles.css
dist/

# ── Entorno ──────────────────────────────────────────────
# El proyecto usa un ÚNICO .env en la raíz (solo credenciales dummy del
# prototipo académico), por lo que SÍ se versiona para que funcione al clonar.
# .env
*.local

# ── Docker / DB ──────────────────────────────────────────
postgres_data/

# ── Editor / OS ──────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
